<div class="container">
  <%= simple_form_for @booking, as: :booking, method: :post, url: slot_book_for_user_path(@slot) do |f| %>
    <%= f.association :user %>
    <%= f.button :submit %>
  <% end %>
</div>